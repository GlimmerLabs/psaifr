# Making the images is easy
; (define render
;   (lambda (fun width height)
;     (image-compute 
;      (lambda (x y)
;        (result2color 
;         (fun (scale x width) (scale y height)))))))

# Making the ecosystem is hard
* Friendly language (visual)
* Features (time, mouse, colors, ...)
* Community
* Primer
* <em>And more ...</em>
